import"../chunks/Bzak7iHL.js";import"../chunks/BSDiJ94n.js";import{j as i,b as l,m as n,W as d,$ as a,k as s,r as c}from"../chunks/BwguTo01.js";var r=i(`<h1 id="migrating-to-newer-smui-versions">Migrating to Newer SMUI Versions</h1> <p>This doc contains information that will help you migrate your code from an older version of SMUI to a newer one. Things that you will need to change in your existing code will be written here. It would also be helpful to check the MDC changelog if the upstream MDC versions has changed between SMUI versions.</p> <h1 id="smui-7---smui-8">SMUI 7 -> SMUI 8</h1> <p>SMUI 8 has migrated to Svelte 5’s new Runes mode. As such, it expects things like snippets and only some props are bindable.</p> <p>Note to Devs: update <code>indexOf()</code> with <code>$state</code> arrays or items to <code>findIndex()</code> to avoid bugs.</p> <p>Note to Devs: remove <code>x = x;</code> statements after <code>push()</code> and the like. They aren’t needed anymore with Svelte 5’s new $state rune.</p> <h2 id="breaking-changes">Breaking Changes</h2> <ul><li>Svelte 5 is required! Svelte 4 will not work.</li> <li>Events have been renamed, removing colons and adopting CamelCase. (ex: SMUISwitch:change to SMUISwitchChange)</li> <li>Event modifiers are now wrapper functions, exported from <code>@smui/common/events</code>.</li> <li>Slots must be migrated to snippets.</li> <li>The deprecated “MDC” events have been removed. All event names should be migrated to the corresponding “SMUI” event names.</li> <li>The “Fixation” theme now uses Tahoma as its large header font.</li> <li>Select no longer defaults the value to an empty string, meaning you must either set the value given to it to an empty string or provide a key function that returns an empty string for an undefined value.</li> <li>TabBar now expects a <code>tab</code> snippet instead of using <code>let:tab</code>.</li> <li>Chips’ Set now expects a <code>chip</code> snippet instead of using <code>let:chip</code>.</li> <li>Chips’ Set key function is now required to return <code>string</code>, not <code>string | number</code>.</li> <li>SegmentedButton now expects a <code>segment</code> snippet instead of using <code>let:segment</code>.</li> <li><code>classAdderBuilder</code> is no longer available. Instead, the <code>ClassAdder</code> component is exported. <ul><li>Svelte 5 doesn’t seem to provide a way to reuse components the way classAdderBuilder needs. The version in beta.2 and before works only on the first render, but props were not reactive.</li></ul></li></ul> <h2 id="changes">Changes</h2> <ul><li>Event listeners for DOM events no longer need to use the CustomEvent type. They can use the correct type, like MouseEvent and KeyboardEvent.</li> <li>New premade theme, “Bubblegum”.</li></ul> <h3 id="components">Components</h3> <ul><li>Chip Input <ul><li>A new component!</li></ul></li></ul> <h1 id="smui-6---smui-7">SMUI 6 -> SMUI 7</h1> <p>SMUI 7 migrated to upstream MDC 14.0 from 13.0: <a href="https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1400-2022-04-27" rel="nofollow">https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1400-2022-04-27</a></p> <h2 id="themes">Themes</h2> <p>I spent a day <a href="https://i.kym-cdn.com/entries/icons/original/000/018/929/graphic_design_is_my_passion.png" rel="nofollow">flexing every graphic design muscle in my body</a>, and made some THEMES! SMUI 7 can now be styled with a custom theme, the default theme, or <a href="https://sveltematerialui.com/INSTALL.md#a-premade-theme" rel="nofollow">brand spanking new premade themes</a>. This makes it much easier to get started with SMUI.</p> <h2 id="breaking-changes-1">Breaking Changes</h2> <ul><li>Tooling now <strong>must</strong> be Svelte-aware. This means adding conditional export names to Rollup and Webpack config. See installation docs.</li> <li>SMUI 7 (after v7.0.0-beta.9) is no longer compatible with Svelte 3. It now requires Svelte 4.</li> <li>If you had started using <code>component=&#123;Svg&#125;</code>, you need to use <code>tag="svg"</code> after v7.0.0-beta.12.</li> <li>The <code>@smui/common/elements</code> components are gone. Just set the <code>tag</code> prop on components now (like <code>&lt;Button tag="div"&gt;</code>). <ul><li>Internally, they are replaced with the <code>SmuiElement</code> component exported from <code>@smui/common</code>. It takes a <code>tag</code> prop and creates an element dynamically with that tag name. You shouldn’t ever need to use <code>SmuiElement</code> directly.</li></ul></li> <li>The ”*ComponentDev” types (like <code>MenuComponentDev</code>) are gone. You can now use the component as its type. Components that can take a <code>component</code> or <code>tag</code> prop (like <code>Button</code>) have required generic arguments that you can use to tell it which tag you’re using.</li> <li>If you’re using <code>classAdderBuilder</code>, you need to use <code>keyof SmuiElementMap</code> instead of <code>string</code> as its generic argument.</li> <li>The <code>dispatch</code> function in <code>@smui/common</code> will now throw an error if either the <code>Event</code> object is not available or the <code>element</code> is not provided.</li> <li>The <code>Input</code> component from the <code>@smui/textfield</code> now sets <code>value</code> to <code>Number.NaN</code> when the input is empty.</li></ul> <h2 id="changes-1">Changes</h2> <h3 id="components-1">Components</h3> <ul><li>Autocomplete <ul><li>The <code>SMUIAutocomplete:selected</code> event is now cancelable.</li></ul></li> <li>Banner <ul><li>New <code>autoClose</code> prop.</li> <li>New <code>SMUIBanner:actionClicked</code> event. It is fired when an action is clicked and <code>autoClose</code> is <code>false</code>.</li></ul></li> <li>Bottom App Bar <ul><li>A new component!</li></ul></li> <li>Dialog <ul><li>New <code>sheet</code> prop.</li> <li>New <code>noContentPadding</code> prop.</li></ul></li> <li>FAB <ul><li>New <code>focusRing</code> prop. Use this to support rendering focused FABs in High Contrast Mode.</li></ul></li> <li>List <ul><li>New <code>disabledItemsFocusable</code> prop.</li> <li>New <code>wrapper</code> prop on Item. This should be used for items that only act as the container of a nested list.</li> <li>New <code>focusItemAtIndex</code> function.</li></ul></li> <li>Menu <ul><li>New <code>SMUIMenu:closedProgrammatically</code> event.</li></ul></li> <li>Menu Surface <ul><li>New <code>SMUIMenuSurface:opening</code> event.</li> <li>New <code>openBottomBias</code> prop.</li></ul></li> <li>Slider <ul><li><code>valueToAriaValueTextFn</code> now gets a <code>thumb</code> argument.</li> <li>New <code>hideFocusStylesForPointerEvents</code> prop. Comes from <a href="https://github.com/material-components/material-components-web/commit/ec54d904621360bcb27e6d3cd1f33112e2a54aac" rel="nofollow">upstream</a>.</li> <li>New <code>minRange</code> prop to limit selectable range.</li></ul></li> <li>Switch <ul><li>New <code>focusRing</code> prop. Use this to support rendering focused switches in High Contrast Mode.</li></ul></li></ul> <h1 id="smui-5---smui-6">SMUI 5 -> SMUI 6</h1> <p>SMUI 6 migrated to upstream MDC 13.0 from 11.0: <a href="https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1300-2021-09-24" rel="nofollow">https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1300-2021-09-24</a></p> <h2 id="breaking-changes-2">Breaking Changes</h2> <ul><li>For <code>@smui/common</code>, <code>@smui/textfield</code>, <code>@smui/select</code>, and <code>@smui/snackbar</code>, if you ever import something like <code>@smui/snackbar/kitchen/index</code> or <code>@smui/textfield/helper-text/index</code>, you need to remove the <code>/index</code> from the end of that import. (This does not apply to Sass imports.) <ul><li>Here’s a regex you can use in VS Code to search for these: @smui/(common|textfield|select|snackbar)/[\\w-]+/index</li></ul></li> <li>The ‘/styled’ import endpoints have been removed. You will need to import the ‘/index’ endpoint from Sass yourself if you were using the Advanced Styling Method.</li></ul> <h3 id="components-2">Components</h3> <ul><li>Switch <ul><li>No longer emits “change” event, use “SMUISwitch:change” event instead.</li></ul></li></ul> <h2 id="changes-2">Changes</h2> <ul><li>MDC events are now dispatched with “SMUI” instead of “MDC” prefix. They are dispatched as both though, so your code shouldn’t break. You should still update your code to use the new “SMUI” prefix anyway.</li> <li>There’s no longer any required Vite config. Big thanks to <a href="https://github.com/benmccann" rel="nofollow">Ben McCann</a> for <a href="https://github.com/hperrin/svelte-material-ui/issues/375#issuecomment-994227865" rel="nofollow">clearing up my misconceptions and helping me fix the package.json files</a>.</li></ul> <h3 id="components-3">Components</h3> <ul><li>Accordion <ul><li>A new component!</li></ul></li> <li>Autocomplete <ul><li>A new component! Huge shoutout to <a href="https://github.com/fiorelorenzo/" rel="nofollow">Lorenzo Fiore</a> who originally authored the component!</li></ul></li> <li>Badge <ul><li>A new component!</li></ul></li> <li>Common <ul><li>New announce function in the “internal” export.</li></ul></li> <li>Paper <ul><li>New variants!</li></ul></li> <li>Switch <ul><li>No longer needs <code>valueKey</code> prop.</li> <li>Default color is now “primary”.</li></ul></li></ul> <h1 id="smui-4---smui-5">SMUI 4 -> SMUI 5</h1> <h2 id="breaking-changes-3">Breaking Changes</h2> <ul><li>TypeScript!!! You don’t need to use TypeScript, but you do need to use the <a href="https://github.com/sveltejs/svelte-preprocess" rel="nofollow">TypeScript preprocessor</a> for Svelte.</li> <li>Events have been renamed from like “SMUI:component:sub-component:event” to “SMUIComponentSubComponent:event”.</li> <li>Elemental components (from @smui/common) should be imported as named imports from <code>@smui/common/elements</code> instead, so that they have the right types.</li> <li>The <code>/bare</code> import paths have been removed. (The default import path has been unstyled since v4.)</li></ul> <h1 id="smui-40---smui-41">SMUI 4.0 -> SMUI 4.1</h1> <ul><li>Event modifiers have changed to use ”$” instead of ”:” (<code>on:click$capture=&#123;handler&#125;</code>), so that all events, even MDC events, can be bound with modifiers. <ul><li>The old ”:” syntax will still work, it will just give a warning in the console.</li></ul></li></ul> <h1 id="smui-3---smui-4">SMUI 3 -> SMUI 4</h1> <h2 id="breaking-changes-4">Breaking Changes</h2> <p>Components no longer import styles from their “index.js” files. If you were using “/bare” before, you don’t need to anymore. If you were relying on styles being imported, you now need to use the “/styled” endpoint for components.</p> <p>Check out the new <a href="https://github.com/hperrin/svelte-material-ui/tree/master/packages/smui-theme" rel="nofollow">smui-theme</a> package. It can help you with your themes.</p> <h1 id="smui-30---smui-31">SMUI 3.0 -> SMUI 3.1</h1> <p>SMUI 3.1 migrated to upstream MDC 11.0 from 10.0: <a href="https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1100-2021-04-15" rel="nofollow">https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1100-2021-04-15</a></p> <p>You should update your MDC dependencies to 11.0 as well.</p> <h2 id="breaking-changes-5">Breaking Changes</h2> <h3 id="theming-sass">Theming, Sass</h3> <ul><li>MDC-Web chips trailing actions class changed.</li> <li>MDC-Web list classes changed.</li></ul> <h2 id="new-features">New Features</h2> <h3 id="components-4">Components</h3> <ul><li>Banner <ul><li>Add focus trap on the banner.</li></ul></li> <li>Dialog <ul><li>New <code>fullscreen</code> prop.</li> <li>New <code>over</code> slot.</li></ul></li> <li>Menu Surface <ul><li>New <code>maxHeight</code> prop.</li> <li>New <code>horizontallyCenteredOnViewport</code> prop.</li></ul></li> <li>Slider <ul><li>Rounds values to correct decimal places based on <code>step</code> prop.</li></ul></li> <li>Tooltip <ul><li>New <code>attachScrollHandler</code> and <code>removeScrollHandler</code> functions for adding additional scrollable elements to handle. Both accept a function.</li></ul></li></ul> <h1 id="smui-2---smui-3">SMUI 2 -> SMUI 3</h1> <p>You will need to migrate to Dart Sass. <code>node-sass</code> is no longer supported, as it is deprecated and too old now.</p> <pre class="highlight"><code class="language-sh">npm un -D node-sass
npm i -D sass</code></pre> <p>SMUI 3 migrated to upstream MDC 10.0 from 4.0: <a href="https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1000-2021-02-05" rel="nofollow">https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#1000-2021-02-05</a></p> <p>There are newly updated instructiong for using SMUI in Sapper.</p> <h2 id="breaking-changes-6">Breaking Changes</h2> <h3 id="theming-sass-1">Theming, Sass</h3> <ul><li>MDC-Web migrated to the “@use” syntax, so mixins, functions, and variables are namespaced by the “@use” statement now instead of a prefix.</li> <li>Theming has changed, so you’ll need to update your ”_smui-theme.scss” file. Follow <a href="/THEMING.md">this guide</a>.</li></ul> <h3 id="components-5">Components</h3> <ul><li>Chips <ul><li>You now need to use <code>let:chip</code> on the set and pass it to the chip with <code>&#123;chip&#125;</code>.</li> <li>Chips now export LeadingIcon, TrailingIcon, and TrailingAction, instead of Icon.</li> <li>Removed <code>selected</code> prop on Chip. <code>selected</code> prop on Chips is now the only way to set the selected chip(s), and it/they should be identical to one/some of the entries in <code>chips</code> (pass objects by reference).</li> <li>Don’t provide any <code>tabindex</code> props to any of the Chips, Texts, or the Set. They will manage their own tabindex.</li> <li>On TrailingAction, use <code>icon$class</code> to add classes to the icon element.</li></ul></li> <li>Data Table <ul><li><code>numeric</code> prop on Cell should now also be set on Head Cells.</li></ul></li> <li>Dialog <ul><li>Open state is now controled by an <code>open</code> prop, instead of <code>open</code> and <code>close</code> functions. There are also <code>isOpen</code> and <code>setOpen</code> functions.</li> <li>Lists should now be placed below <code>Content</code> element, rather than being the <code>Content</code> component. For selection from a list, use the new <code>selection</code> prop on the dialog.</li> <li>Slider dialogs are styled a bit differently. See the <a href="https://sveltematerialui.com/demo/dialog/" rel="nofollow">demo</a>.</li></ul></li> <li>Icon Button <ul><li>No longer emit ‘MDCIconButtonToggle:change’ event without the <code>toggle</code> prop. Instead, listen for click event.</li></ul></li> <li>Linear Progress <ul><li>Removed <code>reversed</code> prop.</li></ul></li> <li>List <ul><li><code>radiolist</code> and <code>checklist</code> props renamed to <code>radioList</code> and <code>checkList</code>.</li></ul></li> <li>Menu Surface <ul><li>Anchor action now takes <code>addClass</code> and <code>removeClass</code> instead of <code>classForward</code> (like Ripple).</li></ul></li> <li>Ripple <ul><li><a href="https://github.com/hperrin/svelte-material-ui/blob/v3.0.0/packages/ripple/README.md#default" rel="nofollow">Ripple’s options</a> have changed considerably.</li></ul></li> <li>Select <ul><li>This is almost completely rewritten. You will need to migrate code for it. Check out the demo page.</li> <li>Helper Text in no longer a component. Instead, a <code>svelte:fragment</code> with text now goes inside the component in the “helper” slot. Its ID and ARIA attributes are set automatically.</li> <li>Removed <code>withLeadingIcon</code> and <code>selectedIndex</code> props.</li> <li>You no longer need <code>withLeadingIcon</code>. Instead, put Icon into the “leadingIcon” slot.</li> <li>You should now set <code>required</code> prop on the Select instead of <code>input$required</code>.</li></ul></li> <li>Slider <ul><li><code>displayMarkers</code> prop renamed to <code>tickMarks</code>.</li> <li><code>step</code> now defaults to 1 (same as the range HTML input).</li></ul></li> <li>Tab <ul><li>Removed <code>active</code> and <code>focusOnActivate</code> props. (No longer needed, since TabBar controls them.)</li></ul></li> <li>TabBar <ul><li>Removed <code>activeIndex</code> prop. <code>active</code> prop is now the only way to set the active tab, and it should be identical to one of the entries in <code>tabs</code> (pass objects by reference).</li></ul></li> <li>Textfield <ul><li>This is almost completely rewritten. You will need to migrate code for it. Check out the demo page.</li> <li>Helper Text and Character Counter now go inside the component in the “helper” slot. Their IDs and ARIA attributes are set automatically.</li> <li>Character Counter in a <code>textarea</code> Textfield goes in the “internalCounter” slot.</li> <li>Removed <code>fullwidth</code>, <code>dense</code>, <code>withLeadingIcon</code>, and <code>withTrailingIcon</code> props.</li> <li>You no longer need <code>withLeadingIcon</code> or <code>withTrailingIcon</code>. Instead, put Icons into the “leadingIcon” and “trailingIcon” slots.</li> <li>You should now set <code>required</code> prop on the Textfield instead of <code>input$required</code>.</li> <li>Manual setup now requires passing the sub-components into Textfield with <code>input</code>, <code>floatingLabel</code>, <code>lineRipple</code>, and <code>notchedOutline</code> props.</li></ul></li></ul> <h2 id="new-features-1">New Features.</h2> <h3 id="theming-sass-2">Theming, Sass</h3> <ul><li>Paper <ul><li>New <code>paper-color</code> mixin.</li></ul></li> <li>Switch <ul><li>New <code>color</code> prop.</li></ul></li></ul> <h3 id="components-6">Components</h3> <ul><li>All components have a <code>getElement</code> function that will return their top level DOM element.</li> <li>All components now forward <strong>all</strong> events, and only listen for them if bounded.</li> <li>They can also take event modifiers with the ”:” syntax istead of ”|” (<code>on:click:capture=&#123;handler&#125;</code>).</li></ul> <p>New Components</p> <ul><li>Banners</li> <li>Circular Progress</li> <li>Layout Grids</li> <li>Segmented Buttons</li> <li>Tooltips</li> <li>Touch Target Wrappers</li></ul> <p>New Features in Existing Components</p> <ul><li>Chips <ul><li>New <code>nonInteractive</code> prop on Set.</li> <li>New <code>shouldFocusPrimaryActionOnClick</code> prop on Chip.</li></ul></li> <li>Common <ul><li>Icon now takes a <code>component</code> prop.</li></ul></li> <li>Data Table <ul><li>New <code>stickyHeader</code> prop.</li> <li>New <code>columnId</code> prop on Cell (defaults to an automatic ID).</li> <li>New <code>rowId</code> prop on Row (defaults to an automatic ID).</li> <li>Pagination</li> <li>Progress Indicator</li> <li>Column Sort Buttons</li></ul></li> <li>Dialog <ul><li>New <code>selection</code> prop.</li></ul></li> <li>Drawer <ul><li>New <code>fixed</code> prop (defaults to true).</li> <li>New <code>isOpen</code> function.</li></ul></li> <li>FAB <ul><li>New <code>touch</code> prop.</li></ul></li> <li>Form Field <ul><li>New <code>noWrap</code> prop.</li></ul></li> <li>Icon Button <ul><li>New <code>ariaLabelOn</code> and <code>ariaLabelOff</code> props for toggle buttons.</li></ul></li> <li>Menu <ul><li>New <code>isOpen</code> function.</li> <li>New <code>setOpen</code> function.</li> <li>New <code>setDefaultFocusState</code> function.</li></ul></li> <li>Menu Surface <ul><li>New <code>fullWidth</code> prop.</li> <li>New <code>anchorMargin</code> prop.</li> <li>New <code>MDCMenuSurface:closing</code> event.</li></ul></li> <li>Select <ul><li>New <code>hiddenInput</code> prop.</li> <li>New <code>key</code> prop for using objects as options.</li></ul></li> <li>Slider <ul><li>New <code>range</code>, <code>start</code>, <code>end</code> props for range sliders.</li> <li>New <code>valueToAriaValueTextFn</code> prop for updating the ARIA value attribute.</li></ul></li> <li>TabBar <ul><li>New <code>tabScroller$align</code> prop.</li></ul></li> <li>Textfield <ul><li>New <code>prefix</code> and <code>suffix</code> props.</li> <li>New <code>validateOnValueChange</code> prop.</li> <li>New <code>input$resizable=&#123;false&#125;</code> prop to disable textarea resizing.</li> <li>New <code>helperLine$</code> ”$” props passed to the helper line.</li></ul></li> <li>Top App Bar <ul><li>New <code>scrollTarget</code> prop.</li></ul></li></ul> <p>If you have trouble upgrading with these instructions, you can raise an issue on GitHub.</p> <h1 id="smui-1---smui-2">SMUI 1 -> SMUI 2</h1> <p>SMUI 2 migrated to upstream MDC 4.0 from 3.0: <a href="https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#400-2019-11-02-1" rel="nofollow">https://github.com/material-components/material-components-web/blob/master/CHANGELOG.md#400-2019-11-02-1</a></p> <ul><li>Chips <ul><li>Text component is now required.</li> <li>Now use their own Text and Icon components. The common ones will not work.</li> <li>Icon no longer requires <code>trailing</code> prop. That is the default.</li> <li>Checkmark component is no longer required. It is added automatically.</li></ul></li> <li>Menu and Menu Surface <ul><li>hoistMenuToBody function removed.</li></ul></li> <li>Select <ul><li><code>enchanced</code> prop is removed. All selects are enhanced now.</li> <li><code>selected</code> prop on Option is no longer required and probably should not be used.</li> <li>You now need to pass a class into <code>anchor$class</code> and <code>menu$class</code> with a width value set.</li></ul></li> <li>Button <ul><li>No longer has a <code>dense</code> prop. Use the mixin from MDC.</li></ul></li></ul>`,1);function u(o){var e=r();n(142),l(o,e)}var p=i('<section class="markdown svelte-1xoo2bu"><!></section>');function f(o){var e=p();d(h=>{a.title="Migrating - SMUI"});var t=s(e);u(t),c(e),l(o,e)}export{f as component};
